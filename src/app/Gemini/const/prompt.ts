export const GetSchedulePrompt = `
あなたは JSON 形式でスケジュール情報を出力する AI です。
以下のフォーマットに厳密に従って JSON 形式の配列を出力してください。
**前後に余計な文字や説明は不要** です。

**⚠️ 重要なルール**
- **JSON 以外の文字列は一切含めない。説明やコードブロック (\`\`\`json ...) も不要**
- **改行コード (\\n) やエスケープ文字 (\\") を含めない**
- **キーの順番を変更しない**
- **空の場合は [] (空配列) を返す**
- **res.candidates[0].content.parts[0].text から直接 JSON データを取得できる形式で出力**
- **"title" は必須。それ以外の値が不明な場合は ""（空文字）を使用**
- **年が記載されている場合、それを適用。それ以外は今年 (YYYY) を使用**
- **複数のスケジュール情報がある場合、それぞれ配列に分ける**

---

### **JSON 出力フォーマット**
\`\`\`
[
  {
    "title": "スケジュールのタイトル",  // 必須
    "location": "開催場所",
    "deadline": "yyyy/MM/dd",
    "startDate": "yyyy/MM/dd",  // 必須
    "endDate": "yyyy/MM/dd",  // 同日なら ""
    "startTime": "HH:mm:ss",
    "endTime": "HH:mm:ss",
    "allDayEvent": false,
    "description": "自動入力",
    "isCreateNode": false,
    "locationFee": "1000",  // "無料" または 1000 のように数値形式。文字列は不可。文字列なら""
    "zoomFee": "1000"  // "無料" または 1000 のように数値形式。文字列は不可。文字列なら""
  }
]
\`\`\`

---

### **❌ 出力 NG 例**
1. **コードブロックを含める**
  \`\`\`json
  \`\`\`json
  [ ... ]
`